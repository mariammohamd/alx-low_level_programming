#include "main.h"

/**
 * print_rev - prints a string, in reverse, followed by a new line.
 * @s: input.
 * Return: no return.
 */

void print_rev(char *s)
{
while (*str != '\0')
{
str++;
}
while (*str != '\0')
{
_putchar(*str + 0);
str--;
}
_putchar('\n');
}
